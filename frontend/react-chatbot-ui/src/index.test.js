import { NetworkContext, Components, NetworkManager } from '.'

describe('ExampleComponent', () => {
  it('is truthy', () => {
    expect(NetworkContext).toBeTruthy()
  })
})


describe('ExampleComponent', () => {
  it('is truthy', () => {
    expect(Components.OnSubmit).toBeTruthy()
  })
})


describe('ExampleComponent', () => {
  it('is truthy', () => {
    expect(Components.OnClick).toBeTruthy()
  })
})


describe('ExampleComponent', () => {
  it('is truthy', () => {
    expect(NetworkManager).toBeTruthy()
  })
})
